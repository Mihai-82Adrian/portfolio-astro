# KoSIT Testsuite Samples

Place curated positive XML samples from `itplr-kosit/xrechnung-testsuite` in this folder.

Requirements for CI gate:
- minimum 5 XML files
- keep source reference in `MANIFEST.md`

The command `npm run verify:xrechnung:kosit` validates this folder with KoSIT after validating generated fixtures.
