# Security Rules

- Do not access sensitive files: ~/.ssh, ~/.npmrc, ~/.config, .env, tokens.
- Do not run destructive commands (rm, sudo, curl, wget) without explicit review.
- Any proposed command must be copy-paste safe and briefly explained.
