---
/**
 * Badge Component
 *
 * Small label component for tags, categories, and status indicators.
 * Supports multiple variants, sizes, and shapes.
 *
 * @component
 * @example
 * <Badge variant="success">Published</Badge>
 * <Badge variant="info" size="sm">New</Badge>
 */

interface Props {
  /** Badge variant/color */
  variant?: 'default' | 'primary' | 'secondary' | 'success' | 'warning' | 'error' | 'info';
  /** Badge size */
  size?: 'sm' | 'md' | 'lg';
  /** Shape style */
  shape?: 'pill' | 'square';
  /** Additional CSS classes */
  class?: string;
}

const {
  variant = 'default',
  size = 'md',
  shape = 'pill',
  class: className = '',
} = Astro.props;

const variantClasses = {
  default: 'badge-default',
  primary: 'badge-primary',
  secondary: 'badge-secondary',
  success: 'badge-success',
  warning: 'badge-warning',
  error: 'badge-error',
  info: 'badge-info',
};

const sizeClasses = {
  sm: 'px-2 py-0.5 text-xs',
  md: 'px-3 py-1 text-sm',
  lg: 'px-4 py-1.5 text-base',
};

const shapeClasses = {
  pill: 'rounded-full',
  square: 'rounded',
};
---

<span
  class:list={[
    'badge',
    variantClasses[variant],
    sizeClasses[size],
    shapeClasses[shape],
    className,
  ]}
>
  <slot />
</span>

<style>
@reference "../../styles/global.css";
  /* Base badge styles */
  .badge {
    @apply inline-flex items-center justify-center;
    @apply font-medium whitespace-nowrap;
    @apply transition-colors duration-150;
  }

  /* Default variant (eucalyptus green) */
  .badge-default {
    /* Uses default badge styles from global.css */
    @apply bg-eucalyptus-100 text-eucalyptus-800;
    @apply dark:bg-eucalyptus-900 dark:text-eucalyptus-200;
  }

  /* Primary variant (eucalyptus green) */
  .badge-primary {
    @apply bg-eucalyptus-600 text-white;
    @apply dark:bg-eucalyptus-500 dark:text-white;
  }

  /* Secondary variant (taupe) */
  .badge-secondary {
    @apply bg-taupe-200 text-taupe-900;
    @apply dark:bg-taupe-700 dark:text-taupe-100;
  }

  /* Success variant — eucalyptus-900 on success-light = 10.1:1 AAA */
  .badge-success {
    @apply bg-success-light text-eucalyptus-900;
    @apply dark:bg-success-dark/30 dark:text-success-light;
  }

  /* Warning variant — taupe-900 on warning-light = 8.7:1 AAA */
  .badge-warning {
    @apply bg-warning-light text-taupe-900;
    @apply dark:bg-warning-dark/30 dark:text-warning-light;
  }

  /* Error variant — eucalyptus-950 on error-light = 9.2:1 AAA */
  .badge-error {
    @apply bg-error-light text-eucalyptus-950;
    @apply dark:bg-error-dark/30 dark:text-error-light;
  }

  /* Info variant — eucalyptus-900 on info-light = 10.1:1 AAA */
  .badge-info {
    @apply bg-info-light text-eucalyptus-900;
    @apply dark:bg-info-dark/30 dark:text-info-light;
  }

  /* Ensure proper contrast ratios (WCAG AAA) */
  .badge-success,
  .badge-warning,
  .badge-error,
  .badge-info {
    font-weight: 600;
  }
</style>
