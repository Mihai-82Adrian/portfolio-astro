---
import Icon from '@components/ui/Icon.astro';

interface Props {
  metrics: Array<{
    label: string;
    value: string;
    icon: string;
    description?: string;
  }>;
  columns?: 2 | 3 | 4;
}

const { metrics, columns = 4 } = Astro.props;
---

<div class="metrics-container">
  <div class={`metrics-grid metrics-cols-${columns}`}>
    {metrics.map((metric) => (
      <div
        class="metric-card"
        title={metric.description || `${metric.label}: ${metric.value}`}
      >
        <div class="metric-icon" aria-hidden="true">
          <Icon name={metric.icon} class="h-6 w-6" />
        </div>
        <div class="metric-content">
          <div class="metric-value">{metric.value}</div>
          <div class="metric-label">{metric.label}</div>
        </div>
        {metric.description && (
          <span class="sr-only">{metric.description}</span>
        )}
      </div>
    ))}
  </div>
</div>

<style>
@reference "../../styles/global.css";
  .metrics-container {
    container-type: inline-size;
    min-width: 0;
    max-width: 100%;
  }

  .metrics-grid {
    display: grid;
    gap: 1rem;
    width: 100%;
    min-width: 0;
    max-width: 100%;
  }

  /* Column layouts */
  .metrics-cols-2 {
    grid-template-columns: 1fr;
  }

  .metrics-cols-3 {
    grid-template-columns: 1fr;
  }

  .metrics-cols-4 {
    grid-template-columns: 1fr;
  }

  @container (min-width: 12rem) {
    .metrics-cols-2 {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .metrics-cols-3 {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .metrics-cols-4 {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @container (min-width: 36rem) {
    .metrics-cols-3 {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .metrics-cols-4 {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  .metric-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 1.5rem 1rem;
    background: color-mix(in srgb, var(--text-primary) 4%, transparent);
    border: 1px solid color-mix(in srgb, var(--text-primary) 12%, transparent);
    border-radius: 0.75rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: default;
    min-width: 0;
    max-width: 100%;
  }

  .metric-card:hover {
    background: color-mix(in srgb, var(--text-primary) 8%, transparent);
    border-color: var(--color-primary);
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
  }

  .metric-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 0.75rem;
    color: color-mix(in srgb, var(--color-primary) 78%, var(--text-primary));
    transition: color 0.3s ease, transform 0.3s ease;
  }

  .metric-card:hover .metric-icon {
    color: var(--color-primary);
    transform: scale(1.1);
  }

  .metric-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    min-width: 0;
    max-width: 100%;
  }

  .metric-value {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
    line-height: 1.2;
    @apply font-sans;
    overflow-wrap: anywhere;
  }

  .metric-label {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-secondary);
    line-height: 1.4;
    @apply font-sans;
    overflow-wrap: anywhere;
  }

  /* Screen reader only text */
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }

  /* Dark theme adjustments (class-based, aligned with site theme toggle) */
  :global(.dark) .metric-card {
    background: color-mix(in srgb, var(--text-primary) 7%, transparent);
    border-color: color-mix(in srgb, var(--text-primary) 16%, transparent);
  }

  :global(.dark) .metric-card:hover {
    background: color-mix(in srgb, var(--text-primary) 12%, transparent);
    border-color: var(--color-primary);
  }

  /* Responsive adjustments */
  @media (max-width: 640px) {
    .metrics-grid {
      gap: 0.75rem;
    }

    .metric-card {
      padding: 1rem 0.75rem;
    }

    .metric-icon {
      margin-bottom: 0.5rem;
    }

    .metric-value {
      font-size: 1.25rem;
    }

    .metric-label {
      font-size: 0.75rem;
    }
  }

  @media (max-width: 768px) {
    .metric-value {
      font-size: 1.375rem;
    }
  }

  /* Print styles */
  @media print {
    .metric-card {
      break-inside: avoid;
      border: 1px solid var(--color-light-border);
      background: white;
    }

    .metric-card:hover {
      transform: none;
      box-shadow: none;
    }
  }

  /* Animation on scroll */
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .metric-card {
    animation: slideUp 0.5s ease-out;
  }

  /* Stagger animation for multiple cards */
  .metric-card:nth-child(1) { animation-delay: 0.1s; }
  .metric-card:nth-child(2) { animation-delay: 0.2s; }
  .metric-card:nth-child(3) { animation-delay: 0.3s; }
  .metric-card:nth-child(4) { animation-delay: 0.4s; }
  .metric-card:nth-child(5) { animation-delay: 0.5s; }
  .metric-card:nth-child(6) { animation-delay: 0.6s; }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    .metric-card {
      animation: none;
      transition: none;
    }

    .metric-card:hover {
      transform: none;
    }
  }

  /* Focus styles for accessibility */
  .metric-card:focus-within {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }
</style>
